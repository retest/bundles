language: shell

jdk: openjdk11

addons:
  apt:
    packages:
    - fakeroot
    - rpm

matrix:
  include:
    - os: linux
      dist: xenial
    - os: osx
      osx_image: xcode10.1

install: ci/install.sh

script: ci/script.sh

deploy:
  provider: script
  script: ci/deploy.sh
